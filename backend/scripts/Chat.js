class ChatEngine {
    constructor(demo)
    {
        this.demo = demo;
        this.LoadEngine();
    }

    LoadEngine() {
        console.log('The engine loading was an success!');
    }
}